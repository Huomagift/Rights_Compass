==Start of OCR for page (\\d+)==([\\s\\S]*?)==End of OCR for page \\1==/g;\nlet match;\nwhile ((match = pageRegex.exec(logContent)) !== null) {\n  const pageNum = parseInt(match[1], 10);\n  const text = match[2];\n  ocrPages.push({ pageNum, text });\n}\n\nocrPages.sort((a, b) => a.pageNum - b.pageNum);\n\nconsole.log(`Extracted ${ocrPages.length} OCR pages from transcript log.`);\n\n// Combine text from page 21 to page 181 (where sections 1-320 live)\nconst constitutionPages = ocrPages.filter(p 
---
==Start of OCR for page')) {\n    try {\n      const parsed = JSON.parse(line);\n      const content = JSON.stringify(parsed);\n      foundText += content + '\\n';\n    } catch (e) {\n      foundText += line + '\\n';\n    }\n  }\n});\n\nrl.on('close', () => {\n  console.log(`Found lines with OCR. Length: ${foundText.length}`);\n  fs.writeFileSync(path.join(__dirname, 'found_lines.txt'), foundText, 'utf-8');\n});\n","Description":"Find OCR lines in transcript_full.jsonl using streaming readline",
---
==Start of OCR for page (\\d+)==([\\s\\S]*?)==End of OCR for page \\1==/g;\nlet m;\nwhile ((m = pageRegex.exec(fullText)) !== null) {\n  const pNum = parseInt(m[1], 10);\n  pages.push({ pageNum: pNum, text: m[2] });\n}\n\npages.sort((a, b) => a.pageNum - b.pageNum);\nconsole.log(`Found ${pages.length} OCR page blocks!`);\n\nif (pages.length > 0) {\n  const constitutionPages = pages.filter(p => p.pageNum >= 21 && p.pageNum <= 181);\n  console.log(`Constitution text pages count (21 to 181): ${cons
---
==Start of OCR for page 21==')) {\n    console.log(`FOUND on line ${lineNum}, length: ${line.length}`);\n    try {\n      const parsed = JSON.parse(line);\n      console.log('Parsed JSON object keys:', Object.keys(parsed));\n      if (parsed.content) {\n        fs.writeFileSync('scratch/ocr_raw_extracted.txt', parsed.content, 'utf-8');\n        console.log('Saved parsed.content to scratch/ocr_raw_extracted.txt');\n      }\n    } catch (e) {\n      console.error('JSON parse error:', e.message);\n
---
==Start of OCR for page')) {\n    ocrBuffer += line + '\\n';\n  }\n});\n\nrl.on('close', () => {\n  console.log(`Read ${lineNum} lines. Found ${ocrBuffer.length} bytes containing OCR text.`);\n  fs.writeFileSync('scratch/all_ocr_lines.jsonl', ocrBuffer, 'utf-8');\n});\n","Description":"Extract all lines containing OCR text from transcript_full.jsonl","Overwrite":true,"TargetFile":"c:\\Users\\Dan\\Desktop\\Huoma\\RIL\\Rights_Compass\\scratch\\extract_all_user_messages.js","toolAction":"Writing li
---
==Start of OCR for page (\\d+)==([\\s\\S]*?)==End of OCR for page \\1==/g;\nlet m;\nwhile ((m = pageRegex.exec(fullText)) !== null) {\n  pageBlocks.push({ pageNum: parseInt(m[1], 10), text: m[2] });\n}\n\npageBlocks.sort((a, b) => a.pageNum - b.pageNum);\nconsole.log(`Successfully extracted ${pageBlocks.length} OCR page blocks!`);\n\nif (pageBlocks.length > 0) {\n  console.log(`Page numbers extracted: ${pageBlocks.map(p => p.pageNum).join(', ')}`);\n}\n","Description":"Unescape and parse all OCR
---
==Start of OCR for page')) {\n    foundCount++;\n    try {\n      const parsed = JSON.parse(line);\n      if (parsed.content) {\n        fullCombinedText += parsed.content + '\\n';\n      }\n    } catch (e) {\n      fullCombinedText += line + '\\n';\n    }\n  }\n});\n\nrl.on('close', () => {\n  console.log(`Found ${foundCount} lines containing OCR text.`);\n  console.log(`Combined text length: ${fullCombinedText.length}`);\n  fs.writeFileSync('scratch/combined_raw_transcript_ocr.txt', fullCombin
---
==Start of OCR for page')) {\n    try {\n      const parsed = JSON.parse(line);\n      console.log('Keys:', Object.keys(parsed));\n      console.log('Type:', parsed.type, 'Source:', parsed.source);\n      if (parsed.content) console.log('Content type:', typeof parsed.content);\n      if (typeof parsed.content === 'string') console.log('Content snippet:', parsed.content.substring(0, 100));\n      if (typeof parsed.content === 'object') console.log('Content object keys:', Object.keys(parsed.conten
---
==Start of OCR for page')) {\n    try {\n      const parsed = JSON.parse(line);\n      const str = JSON.stringify(parsed);\n      allText += str + '\\n';\n    } catch (e) {}\n  }\n});\n\nrl.on('close', () => {\n  console.log(`Raw JSON text size: ${allText.length} bytes.`);\n\n  // Extract all OCR page blocks matching ==Start of OCR for page X== ... ==End of OCR for page X==\n  const pageRegex = /==Start of OCR for page (\\d+)==([\\s\\S]*?)==End of OCR for page \\1==/g;\n  const pages = [];\n  le
---
==Start of OCR for page X== ... ==End of OCR for page X==\n  const pageRegex = /==Start of OCR for page (\\d+)==([\\s\\S]*?)==End of OCR for page \\1==/g;\n  const pages = [];\n  let m;\n  while ((m = pageRegex.exec(allText)) !== null) {\n    const pageNum = parseInt(m[1], 10);\n    // Unescape \\n, \\t, etc.\n    let text = m[2]\n      .replace(/\\\\n/g, '\\n')\n      .replace(/\\\\t/g, '\\t')\n      .replace(/\\\\\"/g, '\"');\n    pages.push({ pageNum, text });\n  }\n\n  // Deduplicate by page
---
==Start of OCR for page (\\d+)==([\\s\\S]*?)==End of OCR for page \\1==/g;\n  const pages = [];\n  let m;\n  while ((m = pageRegex.exec(allText)) !== null) {\n    const pageNum = parseInt(m[1], 10);\n    // Unescape \\n, \\t, etc.\n    let text = m[2]\n      .replace(/\\\\n/g, '\\n')\n      .replace(/\\\\t/g, '\\t')\n      .replace(/\\\\\"/g, '\"');\n    pages.push({ pageNum, text });\n  }\n\n  // Deduplicate by pageNum (keep longest text)\n  const mapByPage = {};\n  for (const p of pages) {\n  
---
==Start of OCR for page', idx)) !== -1) {\n    const snippet = line.substring(idx, idx + 500);\n    foundSnippets.push(snippet);\n    idx += 20;\n  }\n});\n\nrl.on('close', () => {\n  console.log(`Found ${foundSnippets.length} snippets!`);\n  if (foundSnippets.length > 0) {\n    console.log('Sample snippet 1:', JSON.stringify(foundSnippets[0]));\n    console.log('Sample snippet 2:', JSON.stringify(foundSnippets[1]));\n  }\n  fs.writeFileSync('scratch/snippets.txt', foundSnippets.join('\\n---\\n'